import 'package:flutter/material.dart';

class NtftthPayment extends StatelessWidget {
  final String title;

  const NtftthPayment({super.key, required this.title});
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("NT FTTH Payment"),
    );
  }
}
